import SkillsView from "./SkillsView";

import { ITabsProps } from "../../components/Tabs/interfaces/ITabsProps";

const Skills = ({ tabsData }: ITabsProps) => SkillsView({tabsData})

export default Skills;
